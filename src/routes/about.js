import React from 'react';
import "./about.scss"

const about = (props) => {
    return (
        <di className="about">
            about this page......
        </di>
    );
};

export default about;